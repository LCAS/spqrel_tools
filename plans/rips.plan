# enter the arena and goto RIPS location
waitfor_dooropen;
vsay_starting;
goto_entrance;
goto_rips; ! *if* obstaclehere *do* vsay_obstacle; waitfor_not_obstaclehere; restart_action !

# 
vsay_inspectme;

waitfor_screentouched;  # replace with push a specific button on the GUI

# exit the arena
vsay_farewell;
goto_entrance; ! *if* obstaclehere *do* vsay_obstacle; waitfor_not_obstaclehere; restart_action !
goto_exit;


